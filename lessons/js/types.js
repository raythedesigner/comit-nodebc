// var name = 'Jane';
// var age = 31;
var person = {
    name: 'Jane',
    age: 31
};

// console.log(name, typeof(name));
// console.log(age, typeof(age), name + age);

console.log(person.name, typeof(person.name));
console.log(person.age, typeof(person.age), person.name + person.age);

console.log(person, typeof(person));

function example() {
    // silence is golden
}

console.log(example, typeof(example));